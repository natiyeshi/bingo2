export enum MyStates {
  loading = "LODING",
  paused = "PAUSED",
  playing = "PLAYING",
  loaded = "LOADED",
  bingo = "BINGO"
}

export interface ResultIndexType {
  b : number,
  i : number,
  n : number,
  g : number,
  o : number,
}